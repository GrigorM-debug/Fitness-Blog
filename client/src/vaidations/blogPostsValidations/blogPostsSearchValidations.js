export default function blogPostsSearchValidations(postTitle, postCategory) {
    const errors = {};

    // if(!postTitle) {
    //     errors.postTitle = 'Post title is required';
    // }

    // if(postCategory === 'Choose a category') {
    //     errors.postCategory = 'Post category is required !';
    // }

    return errors;
} 